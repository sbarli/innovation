import { DogmaEffect } from '../schemas/dogma-effect.schema';

export const MOCK_DOGMA_EFFECT: DogmaEffect = {
  description: 'Draw a 2.',
  effectTypes: ['draw'],
  isDemand: false,
  isOptional: false,
  repeat: false,
  specialAchievement: undefined,
};
