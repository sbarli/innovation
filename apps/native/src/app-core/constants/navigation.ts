export enum Routes {
  AUTH = '/auth',
  CREATE_ROOM = '/create-room',
  EXAMPLES = '/examples',
  HOME = '/',
  ROOMS = '/rooms',
  ROOM = '/rooms/[roomId]',
}

export enum AuthRouteParams {
  LOGIN = 'login',
  SIGNUP = 'signup',
}
