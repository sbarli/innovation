export enum Routes {
  AUTH = '/auth',
  CREATE_ROOM = '/create-room',
  EXAMPLES = '/examples',
  HOME = '/',
  ROOM = '/rooms/[roomId]',
}

export enum AuthRouteParams {
  LOGIN = 'login',
  SIGNUP = 'signup',
}
